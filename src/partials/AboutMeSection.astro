---
import { ForwardArrowIcon } from "@configs/icons";
import { info } from "@data";

interface Props {
    bodyText: string
}

const { bodyText } = Astro.props
---

<section class="mt-24">
    <h3 class="text-2xl md:text-3xl mb-8 text-fore-primary border-b border-b-slate-200 dark:border-b-gray-600 w-fit">
        À propos de moi
    </h3>
    {info.aboutMe.map((item) => (
        <div
            class="text-fore-primary mb-8 space-y-4"
            set:html={item}
        />
    ))}
    <a
        href="/portfolio/about"
        class="flex items-center text-accent underline underline-offset-2 cursor-pointer hover:opacity-70 transition hover:translate-x-1 w-fit"
    >
        <span class="mr-1">
            <ForwardArrowIcon />
        </span>
        En savoir plus
    </a>
</section>