---
import PostList from "@components/PostList.astro";
import { info } from "@data";
import { FlaskIcon } from "@configs/icons";
---

<section class="mt-24">
    <span class="flex items-center mb-8">
        <div class="bg-back-subtle p-2 mr-4 rounded-full">
            <FlaskIcon />
        </div>
        <h4 class="text-xl text-accent font-semibold">Expérience</h4>
    </span>
    <PostList allPosts={info.posts} postType="works" />
</section>